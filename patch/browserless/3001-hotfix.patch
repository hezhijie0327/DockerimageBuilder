diff --git a/src/browsers/browsers.cdp.ts b/src/browsers/browsers.cdp.ts
index f9b4db2e30a..4450288660f 100644
--- a/src/browsers/browsers.cdp.ts
+++ b/src/browsers/browsers.cdp.ts
@@ -90,6 +90,20 @@ export class ChromiumCDP extends EventEmitter {
       });
 
       if (page) {
+        await page.emulate({
+          viewport: {
+            width: 768,
+            height: 1024,
+            deviceScaleFactor: 2,
+            isMobile: true,
+            hasTouch: true,
+            isLandscape: false
+          },
+          userAgent:
+            'Mozilla/5.0 (iPad; CPU OS 15_0 like Mac OS X) AppleWebKit/605.1.15 ' +
+            '(KHTML, like Gecko) Version/15.0 Mobile/15E148 Safari/604.1'
+        });
+
         this.logger.trace(`Setting up file:// protocol request rejection`);
 
         page.on('error', (err) => {
@@ -229,9 +243,6 @@ export class ChromiumCDP extends EventEmitter {
     }
 
     const patchOptions = [
-      // 预设屏幕尺寸（iPad Mini）
-      '--window-size=768,1024',
-
       // 浏览器参数
       '--disable-crashpad',
       '--disable-crashpad-for-testing',
