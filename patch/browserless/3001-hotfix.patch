diff --git a/src/browsers/browsers.cdp.ts b/src/browsers/browsers.cdp.ts
index bb22eaa272b..ce9e837cd70 100644
--- a/src/browsers/browsers.cdp.ts
+++ b/src/browsers/browsers.cdp.ts
@@ -16,6 +16,8 @@ import {
 import puppeteer, { Browser, Page, Target } from 'puppeteer-core';
 import { Duplex } from 'stream';
 import { EventEmitter } from 'events';
+// 引入 User-Agent 插件
+import UserAgent from 'user-agents';
 // 引入 adblocker 插件
 import AdblockPlugin from 'puppeteer-extra-plugin-adblocker';
 import StealthPlugin from 'puppeteer-extra-plugin-stealth';
@@ -234,6 +236,7 @@ export class ChromiumCDP extends EventEmitter {
       '--disable-crashpad-for-testing',
       '--disable-crashpad-forwarding',
       '--disable-in-process-stack-traces',
+      '--disable-web-security',
       '--no-default-browser-check',
 
       // 反检测增强
@@ -251,13 +254,39 @@ export class ChromiumCDP extends EventEmitter {
       '--single-process',
     ];
 
+    // 通过 User-Agent 随机生成 UA
+    const mobileUA = new UserAgent({
+      deviceCategory: 'mobile'
+    });
+    const { screenWidth, viewportWidth, screenHeight } = mobileUA.data;
+
+    const deviceScaleFactor = screenWidth && viewportWidth 
+      ? (screenWidth / viewportWidth).toFixed(1)
+      : '2';
+
+    const mobileDeviceArgs = [
+      // 移动设备模式
+      '--enable-viewport',
+      '--touch-events=enabled',
+      '--use-mobile-user-agent',
+      // User-Agent 库随机生成的信息
+      `--user-agent=${userAgentData.userAgent}`,
+      `--device-scale-factor=${deviceScaleFactor}`,
+      `--screen-height=${screenHeight}`,
+      `--screen-width=${screenWidth}`,
+      '--window-position=0,0',
+      `--window-size=${screenWidth},${screenHeight}`,
+    ];
+
     const finalOptions = {
       ...options,
       args: [
         `--remote-debugging-port=${this.port}`,
         `--no-sandbox`,
-        // 注入补充 Patch 参数
+        // 注入 patchOptions 参数
         ...patchOptions,
+        // 注入 mobileDeviceArgs 参数
+        ...mobileDeviceArgs,
         ...(options.args || []),
         this.userDataDir ? `--user-data-dir=${this.userDataDir}` : '',
       ].filter((_) => !!_),
