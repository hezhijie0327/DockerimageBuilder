diff --git a/Dockerfile b/Dockerfile
index 534b64337c3..af34bc584ba 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -70,7 +70,6 @@ ENV NODE_OPTIONS="--max-old-space-size=8192"
 WORKDIR /app
 
 COPY package.json pnpm-workspace.yaml ./
-COPY .npmrc ./
 COPY packages ./packages
 COPY patches ./patches
 # bring in desktop workspace manifest so pnpm can resolve it
diff --git a/package.json b/package.json
index 0264acd039e..5a190607c60 100644
--- a/package.json
+++ b/package.json
@@ -97,8 +97,8 @@
     "reinstall:desktop": "rm -rf pnpm-lock.yaml && rm -rf node_modules && pnpm -r exec rm -rf node_modules && pnpm install --node-linker=hoisted",
     "release": "semantic-release",
     "release:branch": "tsx ./scripts/releaseWorkflow/index.ts",
-    "self-hosting:docker": "docker build -t lobehub:local .",
-    "self-hosting:docker-cn": "docker build -t lobehub-local --build-arg USE_CN_MIRROR=true .",
+    "self-hosting:docker": "docker build -t lobehub:local --no-cache .",
+    "self-hosting:docker-cn": "docker build -t lobehub-local --no-cache --build-arg USE_CN_MIRROR=true .",
     "start": "next start -p 3210",
     "stylelint": "stylelint \"src/**/*.{js,jsx,ts,tsx}\" --fix",
     "test": "npm run test-app && npm run test-server",
diff --git a/pnpm-workspace.yaml b/pnpm-workspace.yaml
index b6dbbacf737..abb004ac0e7 100644
--- a/pnpm-workspace.yaml
+++ b/pnpm-workspace.yaml
@@ -43,13 +43,14 @@ packageExtensions:
       postcss-styled-syntax: ^0.7.1
 
 allowBuilds:
+  '@google/genai': true
   '@lobehub/editor': true
   '@vercel/speed-insights': true
   core-js: true
   electron: true
   es5-ext: true
   esbuild: true
-  'ffmpeg-static': true
+  ffmpeg-static: true
   protobufjs: true
   sharp: true
   unrs-resolver: true
diff --git a/src/libs/next/config/define-config.ts b/src/libs/next/config/define-config.ts
index d42ef95fa3d..e78890318a0 100644
--- a/src/libs/next/config/define-config.ts
+++ b/src/libs/next/config/define-config.ts
@@ -1,4 +1,4 @@
-import { existsSync, realpathSync } from 'node:fs';
+import { existsSync } from 'node:fs';
 import { createRequire } from 'node:module';
 import { dirname, relative, resolve, sep } from 'node:path';
 
@@ -32,7 +32,7 @@ const getCommonDirectory = (paths: string[]) => {
 const getTurbopackRoot = () => {
   const nextPackageDirectory = dirname(require.resolve('next/package.json'));
 
-  return getCommonDirectory([realpathSync(process.cwd()), realpathSync(nextPackageDirectory)]);
+  return getCommonDirectory([process.cwd(), nextPackageDirectory]);
 };
 
 const resolvePackageDirectory = (packageName: string) => {
