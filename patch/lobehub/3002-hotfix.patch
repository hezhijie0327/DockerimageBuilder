From 40d466855792b98add6f918f94cc6a2c6b7a959e Mon Sep 17 00:00:00 2001
From: Zhijie He <hezhijie0327@hotmail.com>
Date: Fri, 3 Apr 2026 19:00:28 +0800
Subject: [PATCH] fix: disable button when loding

---
 src/features/PromptRewrite/PromptTransformAction.tsx | 9 ++++-----
 1 file changed, 4 insertions(+), 5 deletions(-)

diff --git a/src/features/PromptRewrite/PromptTransformAction.tsx b/src/features/PromptRewrite/PromptTransformAction.tsx
index a4c1dbe..9cd84dd 100644
--- a/src/features/PromptRewrite/PromptTransformAction.tsx
+++ b/src/features/PromptRewrite/PromptTransformAction.tsx
@@ -35,15 +35,14 @@ const PromptTransformAction = memo<PromptTransformActionProps>(
     const isRewriteLoading = isRewriting && transformAction === 'rewrite';
     const isTranslateLoading = isRewriting && transformAction === 'translate';
 
-    // When rewriting or translating, disable both buttons to prevent multiple simultaneous actions
-    const isRewriteButtonDisabled = isRewriteDisabled || (isRewriting && !isRewriteLoading);
-    const isTranslateButtonDisabled = isRewriteDisabled || (isRewriting && !isTranslateLoading);
+    // Disable all transform buttons while one action is running
+    const isActionDisabled = isRewriteDisabled || isRewriting;
 
     return (
       <Flexbox horizontal gap={4}>
         {isRewriteActionEnabled && (
           <Action
-            disabled={isRewriteButtonDisabled}
+            disabled={isActionDisabled}
             icon={Sparkles}
             loading={isRewriteLoading}
             title={
@@ -55,7 +54,7 @@ const PromptTransformAction = memo<PromptTransformActionProps>(
           />
         )}
         <Action
-          disabled={isTranslateButtonDisabled}
+          disabled={isActionDisabled}
           icon={Languages}
           loading={isTranslateLoading}
           title={
-- 
2.53.0

