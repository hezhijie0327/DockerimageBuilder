diff --git a/packages/business/const/src/index.ts b/packages/business/const/src/index.ts
index bafd6f087998d..388cff873c3c3 100644
--- a/packages/business/const/src/index.ts
+++ b/packages/business/const/src/index.ts
@@ -3,4 +3,4 @@ export * from './branding';
 export * from './llm';
 export * from './url';
 
-export const ENABLE_BUSINESS_FEATURES = false;
+export const ENABLE_BUSINESS_FEATURES = true;
diff --git a/packages/model-bank/src/aiModels/index.ts b/packages/model-bank/src/aiModels/index.ts
index c66c390658938..4b8b3a56240d6 100644
--- a/packages/model-bank/src/aiModels/index.ts
+++ b/packages/model-bank/src/aiModels/index.ts
@@ -122,7 +122,7 @@ export const LOBE_DEFAULT_MODEL_LIST = buildDefaultModelList({
   internlm,
   jina,
   lmstudio,
-  ...(ENABLE_BUSINESS_FEATURES ? { lobehub } : {}),
+  ...(ENABLE_BUSINESS_FEATURES && false ? { lobehub } : {}),
   minimax,
   mistral,
   modelscope,
diff --git a/packages/model-bank/src/modelProviders/index.ts b/packages/model-bank/src/modelProviders/index.ts
index 00fe2cc825170..2baf2b37095d4 100644
--- a/packages/model-bank/src/modelProviders/index.ts
+++ b/packages/model-bank/src/modelProviders/index.ts
@@ -130,7 +130,7 @@ export const LOBE_DEFAULT_MODEL_LIST: ChatModelCard[] = [
 ].flat();
 
 export const DEFAULT_MODEL_PROVIDER_LIST = [
-  ...(ENABLE_BUSINESS_FEATURES ? [LobeHubProvider] : []),
+  ...(ENABLE_BUSINESS_FEATURES && false ? [LobeHubProvider] : []),
   AnthropicProvider,
   GoogleProvider,
   OpenAIProvider,
diff --git a/src/app/[variants]/(auth)/signin/useSignIn.ts b/src/app/[variants]/(auth)/signin/useSignIn.ts
index e149114bcfc1c..47315049e0612 100644
--- a/src/app/[variants]/(auth)/signin/useSignIn.ts
+++ b/src/app/[variants]/(auth)/signin/useSignIn.ts
@@ -1,6 +1,5 @@
 import { ENABLE_BUSINESS_FEATURES } from '@lobechat/business-const';
 import { Form } from 'antd';
-import { useRouter, useSearchParams } from '@/libs/next/navigation';
 import { useEffect, useState } from 'react';
 import { useTranslation } from 'react-i18next';
 
@@ -10,6 +9,7 @@ import { useBusinessSignin } from '@/business/client/hooks/useBusinessSignin';
 import { message } from '@/components/AntdStaticMethods';
 import { requestPasswordReset, signIn } from '@/libs/better-auth/auth-client';
 import { isBuiltinProvider, normalizeProviderId } from '@/libs/better-auth/utils/client';
+import { useRouter, useSearchParams } from '@/libs/next/navigation';
 import { useServerConfigStore } from '@/store/serverConfig';
 import { serverConfigSelectors } from '@/store/serverConfig/selectors';
 
@@ -185,7 +185,7 @@ export const useSignIn = () => {
     setSocialLoading(provider);
     const normalizedProvider = normalizeProviderId(provider);
     try {
-      if (ENABLE_BUSINESS_FEATURES && !(await preSocialSigninCheck())) {
+      if (ENABLE_BUSINESS_FEATURES && false && !(await preSocialSigninCheck())) {
         setSocialLoading(null);
         return;
       }
@@ -248,8 +248,8 @@ export const useSignIn = () => {
     handleSignIn,
     handleSocialSignIn,
     loading,
-    oAuthSSOProviders: ENABLE_BUSINESS_FEATURES ? ssoProviders : oAuthSSOProviders,
-    serverConfigInit: ENABLE_BUSINESS_FEATURES ? true : serverConfigInit,
+    oAuthSSOProviders: ENABLE_BUSINESS_FEATURES && false ? ssoProviders : oAuthSSOProviders,
+    serverConfigInit: ENABLE_BUSINESS_FEATURES && false ? true : serverConfigInit,
     socialLoading,
     step,
   };
diff --git a/src/app/[variants]/(auth)/signup/[[...signup]]/useSignUp.tsx b/src/app/[variants]/(auth)/signup/[[...signup]]/useSignUp.tsx
index e67747bddd9b2..75c8192fac983 100644
--- a/src/app/[variants]/(auth)/signup/[[...signup]]/useSignUp.tsx
+++ b/src/app/[variants]/(auth)/signup/[[...signup]]/useSignUp.tsx
@@ -30,7 +30,7 @@ export const useSignUp = () => {
   const handleSignUp = async (values: SignUpFormValues) => {
     setLoading(true);
     try {
-      if (ENABLE_BUSINESS_FEATURES && !(await preSocialSignupCheck(values))) {
+      if (ENABLE_BUSINESS_FEATURES && false && !(await preSocialSignupCheck(values))) {
         setLoading(false);
         return;
       }
diff --git a/src/app/[variants]/(main)/image/_layout/ConfigPanel/components/ImageNum.tsx b/src/app/[variants]/(main)/image/_layout/ConfigPanel/components/ImageNum.tsx
index 37890ce2eb138..a27ff624952ac 100644
--- a/src/app/[variants]/(main)/image/_layout/ConfigPanel/components/ImageNum.tsx
+++ b/src/app/[variants]/(main)/image/_layout/ConfigPanel/components/ImageNum.tsx
@@ -8,7 +8,7 @@ import { memo, useCallback, useEffect, useMemo, useRef, useState } from 'react';
 import { useImageStore } from '@/store/image';
 import { imageGenerationConfigSelectors } from '@/store/image/selectors';
 
-const DEFAULT_IMAGE_NUM_MAX = ENABLE_BUSINESS_FEATURES ? 8 : 50;
+const DEFAULT_IMAGE_NUM_MAX = ENABLE_BUSINESS_FEATURES && false ? 8 : 50;
 const CUSTOM_VALUE = '__custom__';
 
 interface ImageNumSelectorProps {
diff --git a/src/app/[variants]/(main)/settings/features/SettingsContent.tsx b/src/app/[variants]/(main)/settings/features/SettingsContent.tsx
index 687f764d05b54..a7fe199e55913 100644
--- a/src/app/[variants]/(main)/settings/features/SettingsContent.tsx
+++ b/src/app/[variants]/(main)/settings/features/SettingsContent.tsx
@@ -1,12 +1,12 @@
 'use client';
 
 import { ENABLE_BUSINESS_FEATURES } from '@lobechat/business-const';
-import dynamic from '@/libs/next/dynamic';
 import { Fragment } from 'react';
 
 import Loading from '@/components/Loading/BrandTextLoading';
 import NavHeader from '@/features/NavHeader';
 import SettingContainer from '@/features/Setting/SettingContainer';
+import dynamic from '@/libs/next/dynamic';
 import { SettingsTabs } from '@/store/global/initialState';
 
 const componentMap = {
@@ -111,7 +111,7 @@ const SettingsContent = ({ mobile, activeTab }: SettingsContentProps) => {
         SettingsTabs.Profile,
         SettingsTabs.Stats,
         SettingsTabs.Security,
-        ...(ENABLE_BUSINESS_FEATURES
+        ...(ENABLE_BUSINESS_FEATURES && false
           ? [
               SettingsTabs.Plans,
               SettingsTabs.Funds,
diff --git a/src/app/[variants]/(main)/settings/hooks/useCategory.tsx b/src/app/[variants]/(main)/settings/hooks/useCategory.tsx
index 2192ea4932ce6..1992e404410dd 100644
--- a/src/app/[variants]/(main)/settings/hooks/useCategory.tsx
+++ b/src/app/[variants]/(main)/settings/hooks/useCategory.tsx
@@ -137,7 +137,7 @@ export const useCategory = () => {
       },
     ];
 
-    if (ENABLE_BUSINESS_FEATURES) {
+    if (ENABLE_BUSINESS_FEATURES && false) {
       groups.push({
         items: subscriptionItems,
         key: SettingsGroupKey.Subscription,
diff --git a/src/app/[variants]/(main)/settings/provider/features/ProviderConfig/index.tsx b/src/app/[variants]/(main)/settings/provider/features/ProviderConfig/index.tsx
index 5b8eab8208176..6ea0713768e9e 100644
--- a/src/app/[variants]/(main)/settings/provider/features/ProviderConfig/index.tsx
+++ b/src/app/[variants]/(main)/settings/provider/features/ProviderConfig/index.tsx
@@ -413,7 +413,7 @@ const ProviderConfig = memo<ProviderConfigProps>(
           {extra}
 
           {isCustom && <UpdateProviderInfo />}
-          {canDeactivate && !(ENABLE_BUSINESS_FEATURES && id === 'lobehub') && (
+          {canDeactivate && !(ENABLE_BUSINESS_FEATURES && false && id === 'lobehub') && (
             <EnableSwitch id={id} key={id} />
           )}
         </Flexbox>
diff --git a/src/app/[variants]/layout.tsx b/src/app/[variants]/layout.tsx
index e05f448565258..76fd96acf3d4c 100644
--- a/src/app/[variants]/layout.tsx
+++ b/src/app/[variants]/layout.tsx
@@ -57,7 +57,7 @@ const RootLayout = async ({ children, params }: RootLayoutProps) => {
         )}
       </head>
       <body>
-        {ENABLE_BUSINESS_FEATURES ? (
+        {ENABLE_BUSINESS_FEATURES && false ? (
           <BusinessGlobalProvider>{renderContent()}</BusinessGlobalProvider>
         ) : (
           renderContent()
diff --git a/src/features/ChatInput/ActionBar/STT/index.tsx b/src/features/ChatInput/ActionBar/STT/index.tsx
index efa0526dd75d3..8d3ad1352edfd 100644
--- a/src/features/ChatInput/ActionBar/STT/index.tsx
+++ b/src/features/ChatInput/ActionBar/STT/index.tsx
@@ -15,7 +15,7 @@ const STT = memo<{ mobile?: boolean }>(({ mobile }) => {
 
   if (!enableSTT) return;
 
-  if (ENABLE_BUSINESS_FEATURES) {
+  if (ENABLE_BUSINESS_FEATURES && false) {
     return <BrowserSTT mobile={mobile} />;
   }
   switch (sttServer) {
diff --git a/src/features/CommandMenu/utils/contextCommands.ts b/src/features/CommandMenu/utils/contextCommands.ts
index 0866554d4f530..f2619e1f1840c 100644
--- a/src/features/CommandMenu/utils/contextCommands.ts
+++ b/src/features/CommandMenu/utils/contextCommands.ts
@@ -120,7 +120,7 @@ export const CONTEXT_COMMANDS: Record<ContextType, ContextCommand[]> = {
       path: '/settings/about',
       subPath: 'about',
     },
-    ...(ENABLE_BUSINESS_FEATURES
+    ...(ENABLE_BUSINESS_FEATURES && false
       ? [
           {
             icon: Map,
diff --git a/src/features/Conversation/Error/index.tsx b/src/features/Conversation/Error/index.tsx
index 07792d21619c5..5248671573e82 100644
--- a/src/features/Conversation/Error/index.tsx
+++ b/src/features/Conversation/Error/index.tsx
@@ -3,7 +3,6 @@ import { AgentRuntimeErrorType, type ILobeAgentRuntimeErrorType } from '@lobecha
 import { ChatErrorType, type ChatMessageError, type ErrorType } from '@lobechat/types';
 import { type IPluginErrorType } from '@lobehub/chat-plugin-sdk';
 import { type AlertProps, Block, Highlighter, Skeleton } from '@lobehub/ui';
-import dynamic from '@/libs/next/dynamic';
 import { memo, useMemo } from 'react';
 import { useTranslation } from 'react-i18next';
 
@@ -12,6 +11,7 @@ import useBusinessErrorContent from '@/business/client/hooks/useBusinessErrorCon
 import useRenderBusinessChatErrorMessageExtra from '@/business/client/hooks/useRenderBusinessChatErrorMessageExtra';
 import ErrorContent from '@/features/Conversation/ChatItem/components/ErrorContent';
 import { useProviderName } from '@/hooks/useProviderName';
+import dynamic from '@/libs/next/dynamic';
 
 import ChatInvalidAPIKey from './ChatInvalidApiKey';
 
@@ -117,7 +117,7 @@ interface ErrorExtraProps {
 const ErrorMessageExtra = memo<ErrorExtraProps>(({ error: alertError, data }) => {
   const error = data.error;
   const businessChatErrorMessageExtra = useRenderBusinessChatErrorMessageExtra(error, data.id);
-  if (ENABLE_BUSINESS_FEATURES && businessChatErrorMessageExtra)
+  if (ENABLE_BUSINESS_FEATURES && false && businessChatErrorMessageExtra)
     return businessChatErrorMessageExtra;
 
   if (!error?.type) return;
diff --git a/src/features/FileSidePanel/index.tsx b/src/features/FileSidePanel/index.tsx
index 3c41819fcab2e..66fc4a6ca3a0a 100644
--- a/src/features/FileSidePanel/index.tsx
+++ b/src/features/FileSidePanel/index.tsx
@@ -71,7 +71,7 @@ const FileSidePanel = memo<PropsWithChildren>(({ children }) => {
         }}
       >
         {children}
-        {ENABLE_BUSINESS_FEATURES && <UsageFooter />}
+        {ENABLE_BUSINESS_FEATURES && false && <UsageFooter />}
       </DraggablePanelContainer>
     </DraggablePanel>
   );
diff --git a/src/features/User/UserPanel/PanelContent.tsx b/src/features/User/UserPanel/PanelContent.tsx
index 672ee326b950f..d1cb5d98bd083 100644
--- a/src/features/User/UserPanel/PanelContent.tsx
+++ b/src/features/User/UserPanel/PanelContent.tsx
@@ -66,7 +66,7 @@ const PanelContent = memo<{ closePopover: () => void }>(({ closePopover }) => {
           <Link style={{ color: 'inherit' }} to={'/settings/stats'}>
             <DataStatistics />
           </Link>
-          {ENABLE_BUSINESS_FEATURES && <BusinessPanelContent />}
+          {ENABLE_BUSINESS_FEATURES && false && <BusinessPanelContent />}
         </>
       ) : (
         <UserLoginOrSignup onClick={handleSignIn} />
diff --git a/src/hooks/useTTS.ts b/src/hooks/useTTS.ts
index c4cf3ee68a1b8..1d6f866990d5c 100644
--- a/src/hooks/useTTS.ts
+++ b/src/hooks/useTTS.ts
@@ -41,7 +41,9 @@ export const useTTS = (content: string, config?: TTSConfig) => {
       options = {
         api: {
           headers: createHeaderWithOpenAI(),
-          serviceUrl: API_ENDPOINTS.tts(ENABLE_BUSINESS_FEATURES ? businessTTSProvider : 'openai'),
+          serviceUrl: API_ENDPOINTS.tts(
+            ENABLE_BUSINESS_FEATURES && false ? businessTTSProvider : 'openai',
+          ),
         },
         options: {
           model: ttsSettings.openAI.ttsModel,
diff --git a/src/layout/GlobalProvider/index.tsx b/src/layout/GlobalProvider/index.tsx
index a8a5738b94341..f11897b754581 100644
--- a/src/layout/GlobalProvider/index.tsx
+++ b/src/layout/GlobalProvider/index.tsx
@@ -81,7 +81,7 @@ const GlobalLayout = async ({
                 </FaviconProvider>
               </QueryProvider>
               <Suspense>
-                {ENABLE_BUSINESS_FEATURES ? <ReferralProvider /> : null}
+                {ENABLE_BUSINESS_FEATURES && false ? <ReferralProvider /> : null}
                 <ImportSettings />
                 {process.env.NODE_ENV === 'development' && <DevPanel />}
               </Suspense>
diff --git a/src/libs/better-auth/define-config.ts b/src/libs/better-auth/define-config.ts
index fe3b3c46ef0bc..04d7837ca603f 100644
--- a/src/libs/better-auth/define-config.ts
+++ b/src/libs/better-auth/define-config.ts
@@ -66,7 +66,7 @@ const enabledSSOProviders = parseSSOProviders(authEnv.AUTH_SSO_PROVIDERS);
 const { socialProviders, genericOAuthProviders } = initBetterAuthSSOProviders();
 
 async function customEmailValidator(email: string): Promise<boolean> {
-  return ENABLE_BUSINESS_FEATURES ? businessEmailValidator(email) : validateEmail(email);
+  return ENABLE_BUSINESS_FEATURES && false ? businessEmailValidator(email) : validateEmail(email);
 }
 
 interface CustomBetterAuthOptions {
diff --git a/src/server/globalConfig/index.ts b/src/server/globalConfig/index.ts
index 6286d36650aba..a1caf985b3f4c 100644
--- a/src/server/globalConfig/index.ts
+++ b/src/server/globalConfig/index.ts
@@ -30,8 +30,8 @@ export const getServerGlobalConfig = async () => {
   const { DEFAULT_AGENT_CONFIG } = getAppConfig();
 
   const config: GlobalServerConfig = {
-    aiProvider: await genServerAiProvidersConfig({
-      ...(ENABLE_BUSINESS_FEATURES
+    aiProvider: await genServerAiProvidersConfig( {
+      ...(ENABLE_BUSINESS_FEATURES && false
         ? {
             lobehub: {
               enabled: true,
diff --git a/src/server/routers/async/image.ts b/src/server/routers/async/image.ts
index 332b573e7ec72..cd22e13a9befc 100644
--- a/src/server/routers/async/image.ts
+++ b/src/server/routers/async/image.ts
@@ -266,7 +266,7 @@ export const imageRouter = router({
 
           const { modelUsage } = response;
 
-          if (ENABLE_BUSINESS_FEATURES) {
+          if (ENABLE_BUSINESS_FEATURES && false) {
             await chargeAfterGenerate({
               metadata: {
                 asyncTaskId: taskId,
diff --git a/src/server/routers/lambda/userMemories.ts b/src/server/routers/lambda/userMemories.ts
index c061a300c2b41..d6e4719b3e5b9 100644
--- a/src/server/routers/lambda/userMemories.ts
+++ b/src/server/routers/lambda/userMemories.ts
@@ -140,7 +140,7 @@ const getEmbeddingRuntime = async (serverDB: LobeChatDatabase, userId: string) =
   const agentRuntime = await initModelRuntimeFromDB(
     serverDB,
     userId,
-    ENABLE_BUSINESS_FEATURES ? BRANDING_PROVIDER : provider,
+    ENABLE_BUSINESS_FEATURES && false ? BRANDING_PROVIDER : provider,
   );
 
   return { agentRuntime, embeddingModel };
diff --git a/src/server/services/user/index.ts b/src/server/services/user/index.ts
index 526540cfa2e06..4d186410f9dd7 100644
--- a/src/server/services/user/index.ts
+++ b/src/server/services/user/index.ts
@@ -1,6 +1,5 @@
- 
-import { ENABLE_BUSINESS_FEATURES } from '@lobechat/business-const';
 import { type UserJSON } from '@clerk/backend';
+import { ENABLE_BUSINESS_FEATURES } from '@lobechat/business-const';
 import { type LobeChatDatabase } from '@lobechat/database';
 
 import { initNewUserForBusiness } from '@/business/server/user';
@@ -28,7 +27,7 @@ export class UserService {
   }
 
   async initUser(user: CreatedUser) {
-    if (ENABLE_BUSINESS_FEATURES) {
+    if (ENABLE_BUSINESS_FEATURES && false) {
       try {
         await initNewUserForBusiness(user.id, user.createdAt);
       } catch (error) {
diff --git a/src/store/chat/slices/aiChat/actions/conversationLifecycle.ts b/src/store/chat/slices/aiChat/actions/conversationLifecycle.ts
index df3b4a2ca8c67..e2044d9db71ba 100644
--- a/src/store/chat/slices/aiChat/actions/conversationLifecycle.ts
+++ b/src/store/chat/slices/aiChat/actions/conversationLifecycle.ts
@@ -325,7 +325,7 @@ export const conversationLifecycle: StateCreator<
       get().updateOperationMetadata(operationId, { inputEditorTempState: null });
     }
 
-    if (ENABLE_BUSINESS_FEATURES) {
+    if (ENABLE_BUSINESS_FEATURES && false) {
       markUserValidAction();
     }
 
diff --git a/src/store/image/slices/createImage/action.ts b/src/store/image/slices/createImage/action.ts
index f794723d299a6..c86c1931f6c6d 100644
--- a/src/store/image/slices/createImage/action.ts
+++ b/src/store/image/slices/createImage/action.ts
@@ -74,7 +74,7 @@ export const createCreateImageSlice: StateCreator<
         set({ isCreatingWithNewTopic: true }, false, 'createImage/startCreateImageWithNewTopic');
       }
 
-      if (ENABLE_BUSINESS_FEATURES) {
+      if (ENABLE_BUSINESS_FEATURES && false) {
         markUserValidAction();
       }
 
